#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: the character to check
 * return: 1 if c is lowercase , 0 otherswise
 */
int _islowercase(int c)
{
return(c >= 'a' && c<= 'Z');
}
